# An adventure game!

I wanted to make a horror game using scriptable objects.

It works pretty well, altho it doesn't seem like the most efficient way to code. (I used a lot of booleans)

I'd like to take it farther and maybe include choices. For example "Take key?" and "Use key?" instead of it just happening in a particular order. 
That way the player can make more mistakes/learn the environment
